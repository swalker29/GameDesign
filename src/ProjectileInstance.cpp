#include "ProjectileInstance.hpp"

ProjectileInstance::ProjectileInstance(int projectileIndex, sf::Vector2f position, sf::Vector2f direction) : projectileIndex(projectileIndex), position(position), direction(direction) {

}

// Default destructor
